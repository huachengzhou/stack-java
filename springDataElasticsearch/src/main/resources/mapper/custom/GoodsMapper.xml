<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.my.mapper.custom.GoodsMapper">

    <select id="findAll" resultType="com.my.jpa.GoodsSQLServer">
        SELECT
        id,
        title,
        price,
        stock,
        saleNum,
        createTime,
        categoryName,
        brandName,
        status
        FROM
        goods
    </select>

</mapper>
